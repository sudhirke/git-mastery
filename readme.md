Welcome to Git

- This is coming from dev-sudhirke branch
- Always commit with a better commit message "When this code changes applied,  it will..."
- git push origin <branch> : this will push the branch to remote repo.

## Topics learned today
- git init
- git add ./
- git commit -m 'message for the commit'

- git branch <branchname>
- git checkout <branchname>

- git reset <hashcode>
- git revert <hashcode>
- git stash
- git stash apply stash@{0}